// Copy this file to config.js and fill in your Copilot Studio token endpoint.
//
// To find your token endpoint:
// 1. Open Copilot Studio → Settings → Channels → Mobile app
// 2. Copy the "Token Endpoint" URL
//
// config.js is gitignored — it will not be committed.

const CONFIG = {
  tokenEndpoint: 'https://YOUR_ENVIRONMENT.REGION.environment.api.powerplatform.com/powervirtualagents/botsbyschema/YOUR_BOT_SCHEMA/directline/token?api-version=2022-03-01-preview',
};
